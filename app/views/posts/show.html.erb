<p id="notice"><%= notice %></p>

<%= @user_id %>
<%= @user %>
<%= @user.meta %>
<%= @user.meta.id %>
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<p>

<% if @user.meta.id == @post.organization_id %>
<%= link_to 'Destroy', @post, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Edit', edit_post_path(@post) %> |
<% end %>
<%= link_to 'Back', posts_path %>
